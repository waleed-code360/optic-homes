# Optic Homes — Mobile Fixed

Upload `index.html` and `.nojekyll` to the root of the existing GitHub repository,
replacing the previous `index.html`.

Fixes included:
- Removed right-side horizontal overflow
- Full-width Android/iPhone layout
- Safer hidden cart drawer
- Responsive prescription form
- Horizontally scrollable prescription table
- Improved small-screen header and buttons
- Safe-area support
